# Frontend calls /api by default. Next proxies /api/* to BACKEND_API_URL.
BACKEND_API_URL=http://localhost:4000

# Optional: use only if you want the browser to call the backend directly.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
